# Backend ConfigMap - Environment configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: threat-modeler
  labels:
    app: backend
data:
  PORT: "8080"
  MAX_FILE_SIZE: "10485760"  # 10MB
  ENABLE_DATAFLOW_ANALYSIS: "true"
  LOG_LEVEL: "info"
